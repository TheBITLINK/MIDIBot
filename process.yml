apps:
  - name   : midiBot
    interpreter: coffee
    script : main.coffee
    env    :
      NODE_ENV: production
      BOT_MODULES: '["admin", "ping", "eval", "convert"]'
      
    env_staging:
      NODE_ENV: development
      PORT: 3052
