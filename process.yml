apps:
  - name   : midiBot
    interpreter: ./node_modules/.bin/coffee
    script : main.coffee
    instances: max
    exec_mode: cluster
    env    :
      NODE_ENV: production      
    env_staging:
      NODE_ENV: development
      PORT: 3052
